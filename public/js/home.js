jQuery(function($){
	var $win 		= $(window);
	var $container  = $('#main');
	var $vr 		= $('.vr');
	var $bar 		= $('.bar');

/*
	$container.height($win.height());

	_automated = true;

  	$win.on('resize', function() {
  		var winHeight = $win.height();
  		$container.height(winHeight);

  		var vpos = $vr.offset();

  		if(vpos) {
		  	var vspace = winHeight - 30 - vpos.top;
	  		$vr.height( vspace );
	  		$container.css('paddingTop', vspace / 2);
  		}
  	}).resize();

  	setTimeout(function(){
  		_automated = true;
  		$win.resize();
  		_automated = false;
  	}, 900);

  	_automated = false;

    */
});